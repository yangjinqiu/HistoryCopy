# 历史粘贴板 — 产品需求文档 (PRD)

> 版本: v1.0 | 日期: 2026-05-10

---

## 1. 产品概述

一款运行在 macOS 上的历史剪贴板管理工具。自动记录用户复制的所有文字和图片内容，支持按时间倒序浏览、搜索、置顶、删除，通过菜单栏常驻并可随时通过全局快捷键调出。

**核心价值**：再也不会丢失复制过的内容。

---

## 2. 功能需求

### 2.1 自动记录
- 实时监听系统剪贴板变化（每 0.5~1 秒轮询一次）
- 记录 **纯文本** 和 **图片** 两种内容类型
- 记录附带时间戳
- 内容去重：连续复制相同内容不重复存储，但刷新该条目的时间戳（提到最上方）

### 2.2 存储与清理
- **存储位置**：仅本地存储，不联网
- **最大条数**：1000 条（超出时自动删除最旧的条目，已置顶的除外）
- **保留时长**：用户可设置（见 2.3）
- 过期内容自动清理（已置顶的除外）

### 2.3 用户设置
| 设置项 | 默认值 | 说明 |
|--------|--------|------|
| 保留时长 | 3 天 | 可选：1天 / 3天 / 5天 / 7天 / 永不 |
| 最大条数 | 1000 | 固定上限 |
| 全局快捷键 | `Cmd + Option + V` | 用户可自定义，支持冲突检测 |
| 开机自启 | 开启 | 可关闭 |

### 2.4 卡片操作
每一条复制内容展示为一张卡片：
- **置顶** 📌：固定该卡片，置顶项不会被自动清理
- **删除** 🗑：手动移除此条记录
- **再次复制**：点击卡片内容区域，将内容重新写入系统剪贴板

### 2.5 排序与展示
- 按复制时间 **降序** 排列（最新在最上方）
- 置顶卡片始终排在最上方，其次按时间降序
- 卡片预览：文字显示前 2~3 行截断；图片显示缩略图
- 点击卡片可展开查看完整内容

### 2.6 搜索
- 搜索框位于列表顶部
- 输入关键词实时过滤，匹配文本内容
- 搜索范围：文本内容的模糊匹配（不区分大小写）
- 图片暂不支持内容搜索（无 OCR）

### 2.7 全局快捷键呼出
- **默认快捷键**：`Cmd + Option + V`
- 任意应用内按下快捷键，在屏幕中央弹出历史面板
- 再次按下相同快捷键或点击面板外部区域，面板收起
- 快捷键支持用户自定义，修改时检测是否与系统/已有快捷键冲突

### 2.8 菜单栏
- 菜单栏显示一个简洁图标（如 📋 或自定义图标）
- **左键点击图标**：弹出历史面板
- **右键点击图标**：显示菜单
  - 暂停记录 / 恢复记录
  - 设置
  - 退出

---

## 3. UI 设计

### 3.1 整体风格
- macOS 原生风格，采用 **毛玻璃模糊背景**（`.ultraThinMaterial` / vibrancy）
- 圆角卡片、柔和阴影
- 浅色/深色模式自动跟随系统
- 字体使用系统 San Francisco

### 3.2 布局结构（从上到下）
```
┌─────────────────────────────┐
│  🔍 搜索...          ⚙️ 设置  │   ← 顶部栏（搜索 + 设置入口）
├─────────────────────────────┤
│  📌 置顶卡片（如有）          │
│  ┌─────────────────────┐   │
│  │ 📝 [文字/图片内容]    │   │   ← 可滑动列表
│  │    2分钟前  📌 🗑    │   │
│  └─────────────────────┘   │
│  ┌─────────────────────┐   │
│  │ 🖼 [缩略图]          │   │
│  │    5分钟前  📌 🗑    │   │
│  └─────────────────────┘   │
│  ...更多卡片...             │
├─────────────────────────────┤
│  共 156 条  |  保留3天       │   ← 底部状态栏
└─────────────────────────────┘
```

### 3.3 面板规格
- 宽度：约 380px
- 最大高度：约 600px（超出滚动）
- 弹出位置：屏幕中央偏上
- 动画：缩放 + 淡入（约 0.2s）

### 3.4 空状态
当没有历史记录时，显示引导文字："暂无复制记录，开始复制吧 ✨"

---

## 4. 技术方案（建议）

| 维度 | 方案 |
|------|------|
| 开发语言 | Swift + SwiftUI |
| 最低系统 | macOS 14 (Sonoma) |
| 剪贴板监听 | `NSPasteboard` 轮询 + `changeCount` 检测 |
| 数据存储 | Core Data / SwiftData（本地 SQLite） |
| 图片存储 | 文件系统（`Application Support` 目录），数据库仅存路径 |
| 菜单栏 | `NSStatusBar` + SwiftUI `MenuBarExtra` |
| 全局快捷键 | `KeyboardShortcuts` 库或 `CGEvent` API |
| 毛玻璃效果 | SwiftUI `.background(.ultraThinMaterial)` |
| 打包分发 | DMG 安装包 |

---

## 5. 已确认事项

1. **应用名称**：HistoryCopy
2. **菜单栏图标**：定制图标，透明质感，贴合 macOS 玻璃风格
3. **面板开关**：按一下快捷键弹出在屏幕正中央，点击空白区域关闭
4. **图片搜索**：暂不做 OCR，仅搜索文本内容
5. **再次打开面板**：回到顶部（不记忆上次滚动位置）

---

## 6. 开发计划

| 阶段 | 内容 |
|------|------|
| Phase 1 | 剪贴板监听 + 基础存储 + 文字记录 |
| Phase 2 | UI 面板 + 卡片列表 + 时间排序 |
| Phase 3 | 搜索 + 置顶 + 删除 + 再复制 |
| Phase 4 | 图片支持 + 菜单栏 + 全局快捷键 |
| Phase 5 | 设置面板 + 快捷键自定义 + 打包发布 |

---

> 请审核以上内容，尤其是第 5 节的待确认事项。确认后我开始着手开发。
